<div class="tab-pane fade <%= 'show active' if tab_id == 'all-courses' %>" id="<%= tab_id %>" role="tabpanel" aria-labelledby="<%= tab_id %>-tab">
  <div class="row row-cols-1 row-cols-md-4 g-4 m-2">
    <%= render 'course_cards', courses: courses %>
  </div>
</div>
